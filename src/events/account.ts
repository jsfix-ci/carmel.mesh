import { Session } from '..'

export const create_account_event = async (session: Session, event: any) => {
}

export const create_account_result_event = async (session: Session, event: any) => {
}

export const update_account_event = async (session: Session, event: any) => {
}

export const update_account_result_event = async (session: Session, event: any) => {
}